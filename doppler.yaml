# Doppler project config — names only, no secrets.
# This file is optional. The server reads standard env vars regardless of how
# they are injected (shell export, .env, Doppler, MCP client config, etc.).
# See docs/reference/comet-api.md for the full env var reference.

setup:
  project: secrets_managment
  config: dev

flags:
  analytics: false
  env-warning: false
  update-check: false
